%table.table.table-bordered
  %thead
    %th 月
    %th 決済方法
    %th 支払先
    %th 金額
    %th メモ
    %thead
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.month
        %td= post.method
        %td= post.payee
        %td= number_to_currency(post.amount, unit: "円", strip_insignificant_zeros: true)
        %td= post.note
/= paginate @posts
